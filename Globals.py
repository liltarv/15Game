class Globals:
    
    #RESX and RESY are the resolution of the window
    #ROWS and COLS are the number of rows and columns in the grid
    def __init__(self, RESX, RESY, ROWS, COLS):
        self.DIMX = RESX
        self.DIMY = RESY
        self.ROWS = ROWS
        self.COLS = COLS